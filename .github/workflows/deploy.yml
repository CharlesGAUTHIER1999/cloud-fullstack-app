name: Deploy Appcloud

on:
  push:
    branches: ["main"]

jobs:
  deploy-backend:
    name: Deploy Backend (Laravel) to Cloud Run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Decode and setup GCP credentials
        run: |
          echo "${{ secrets.GCP_SA_KEY_BASE64 }}" | base64 --decode > ${HOME}/gcp-key.json
          gcloud auth activate-service-account --key-file=${HOME}/gcp-key.json
          gcloud config set project ynovtestv2
          gcloud auth list

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ynovtestv2
          credentials_json: ${{ secrets.GCP_SA_KEY_BASE64 }}

      - name: Configure Docker auth
        run: gcloud auth configure-docker europe-west1-docker.pkg.dev --quiet

      - name: Deploy Laravel API to Cloud Run
        run: |
          cd backend
          gcloud run deploy appcloud-backend \
            --source . \
            --region europe-west1 \
            --platform managed \
            --allow-unauthenticated \
            --project ynovtestv2

  deploy-frontend:
    name: Deploy Frontend (React) to Vercel
    runs-on: ubuntu-latest
    needs: deploy-backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build project
        run: |
          cd frontend
          npm run build

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          cd frontend
          npx vercel --token $VERCEL_TOKEN --prod --yes \
            --scope $VERCEL_ORG_ID \
            --name cloud-fullstack-app